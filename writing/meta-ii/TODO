compiler:

- whitespace should be handled in grammar
- repetitions: ? + *
- assign match to names instead of using %
- get rid of regex matching

documentation:

- names make sense?
- remove unnecessary steps?
- produce source diffs
