Calculator {
  expression =
    | additive
  additive =
    | multitive:x '+' additive:x -> add(x, y)
    | multitive
  multitive =
    | digit:x '*' multitive:y    -> mul(x, y)
    | digit
  digit =
    | '0'-'9':x                  -> int(x)
}
