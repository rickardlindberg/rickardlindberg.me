TreePrinter {
  toHtmlList =
    | toHtmlListItem*:xs     -> { "<ul>" xs "</ul>" }
  toHtmlListItem =
    | node:x                 -> { "<li>" x "</li>"  }
  node =
    | [atom:x toHtmlList:ys] -> { x ys              }
    | atom
  atom =
    | .:x                    -> { escape(str(x))    }
}
